import tailwindPlugin from "prettier-plugin-tailwindcss";

/** @type {import("prettier").Config} */
const config = {
  plugins: [tailwindPlugin],
  // Add any other Prettier configurations you need here
};

export default config;
